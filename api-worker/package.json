{
    "name": "miquel-api",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "deploy": "wrangler deploy --env production",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "cf-typegen": "wrangler types"
    },
    "devDependencies": {
        "typescript": "^5.5.2",
        "wrangler": "^4.51.0"
    },
    "dependencies": {
        "hono": "^4.10.3"
    }
}
